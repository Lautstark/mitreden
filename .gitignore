# Your own phrases and everything made from them — personal, stays local.
# Start from phrases.example.json, which is the only phrase file in the repo.
phrases.json
phrases.json.*
out/
build/

# Audio never belongs in here, wherever it turns up.
*.wav
*.mp3
*.aiff

# Credentials
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/

# Cleanup folder of the Cowork sandbox
_to_delete/

# System
.DS_Store
