{
  "extends": "@lautstark/toolchain/tsconfig.json",
  "//": "The base is @lautstark/toolchain's since 2026-09-16 - target, lib, module, strictness, the lot, identical to what this file used to write out and to what bildhaft and vorlaut-editor each wrote separately. What is left here is this product's own: which types are ambient, and what is checked.",
  "compilerOptions": {
    "types": [
      "vite/client",
      "node"
    ]
  },
  "//include": "The suites are checked too, not just src. They were outside this list until 2026-08-28, so `npm run typecheck` never read a line of them and vitest and Playwright only ever transpiled them - which is how tests/unit/backup-payload.test.ts came to build a Phrase with `collections: [...]`, a field version 4 removed. It stored two sentences in no Sammlung at all, passed, and said it was testing two in one.",
  "include": [
    "src",
    "tests",
    "e2e"
  ]
}
