# Nothing personal in the image: the phrases, everything made from them and
# the key all come from the mounted directory at runtime.
phrases.json
phrases.json.*
out/
build/
.env
.env.*

*.wav
*.mp3
*.aiff

.git
.github
.claude
__pycache__/
*.pyc
.venv/
.DS_Store

# The tests are not part of the image.
tests/

# Nor is the website. docs/ is the landing page and the browser-only build of
# mitreden, including 28 MB of vendored wasm that the container has no use for
# — it speaks with the piper it installed itself. tools/ builds that site.
docs/
tools/
